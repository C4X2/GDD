package com.emerald.gdd.common.params.model;

/**
 * An interface to mark all singleton classes within the application. However
 * all classes that implement Singleton<T> as a supertype should define a static
 * method instance() that returns a instance of the type T.
 *
 * @author C4X2
 * @version 1.0
 * @param <T> the Singleton class
 */
public interface Singleton<T>
{
	
}
